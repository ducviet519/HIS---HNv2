@model IEnumerable<System.App.Entities.LCD>
<script type="text/javascript">
        $(function () {

        });
</script>

<table class="table table-bordered">
    @if (Model.Count() > 0)
    {
        foreach (var item in Model)
        {
    <tr>
        <td class="text-center" width="90">@item.SOVAOVIEN</td>
        <td>@item.TENBN</td>
    </tr>
        }
    }
</table>